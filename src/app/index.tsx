/**
 * /app/index.tsx  →  Splash Screen route  (Expo Router entry point)
 *
 * The actual UI lives in src/features/auth/SplashScreen.tsx
 * so it stays cleanly inside the feature folder structure.
 */
export { default } from '@/features/auth/splash-screen';
